using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AxeAttack : EnemyAttack
{
    // Start is called before the first frame update
    void Start()
    {
        //normalDamage = 10;
        normalScope = 0.2f;
    }

  
}
