using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoblinSwordAttack : EnemyAttack
{
    // Start is called before the first frame update
    void Start()
    {
        normalDamage = 1;
        normalScope = 0.2f;
    }

    
}
